import { combineReducers } from "redux";
import setSelectedProductID from "./setSelectedProductIDReducer";

const rootReducer = combineReducers({setSelectedProductID});

export default rootReducer;

// export default combineReducers({setSelectedProductID});

